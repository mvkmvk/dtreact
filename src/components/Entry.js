import React from "react";

function Entry(props) {
  return(
    <div class="flexbox-container">
        <span>{props.zin1}</span>
        <span>{props.zin2}</span>
    </div>
  )
}

export default Entry;
